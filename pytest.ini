[pytest]
testpaths = tests
python_files = test_*.py
cache_dir = .local/pytest-cache
markers =
    contract: deterministic route, API, and access-policy contract checks
    path_boundary: generated filesystem path budget and portability contracts
    windows_host: tests owned by the exact Windows host lane of candidate-gate
