# TypeScript Backend Cutover Readiness

Last updated: 2026-06-28

Status: planning and validation-gap matrix for the TypeScript backend rewrite

## Operating Boundary

Flask remains the production authority. This document does not approve a PR, merge,
deploy, live data write, Fly sync, or production cutover. It defines the remaining
evidence needed before those user-approved steps can be considered.

This lane intentionally owns documentation only. It should not edit active
implementation files such as `apps/api/src/content/characterAuthoring.ts`,
`apps/api/src/combat/view.ts`, `apps/api/src/server.ts`, or `apps/api/src/routes.ts`.

The ignored `.local` directory is not present in this worktree, so the active local
roadmap file `./.local/roadmaps/typescript-backend-rewrite-roadmap.md` was not
available for this pass. The matrix below is derived from tracked docs and current
contract evidence only.

## Sources Reviewed

- `docs/typescript-backend-rewrite/charter.md`
- `docs/typescript-backend-rewrite/README.md`
- `docs/typescript-backend-rewrite/content-character-staging-readiness.md`
- `docs/typescript-backend-rewrite/route-snapshots.md`
- `docs/typescript-backend-rewrite/parity-inventory.md`
- `docs/typescript-backend-rewrite/sqlite-migration-spike.md`
- `docs/typescript-backend-rewrite/read-only-compatibility-slice.md`
- `docs/api-v1.md`
- `docs/current-state/INDEX.md`
- `docs/current-state/workspace-boundaries.md`
- `docs/current-state/ops-deploy.md`
- `docs/current-state/frontend-gen2.md`
- `docs/current-state/admin-auth.md`

## Gate Summary

| Gate | Category | Current evidence | Remaining readiness gap | Proof required before PR/merge/deploy/cutover |
| --- | --- | --- | --- | --- |
| API route inventory stays in lockstep | Route parity | `route-snapshots.json`, `route-snapshots.md`, and `apps/api/tests/route-parity.mjs` exist; `/api/v1` snapshot lists 135 Flask API declarations. | Keep route seed, implemented manifest, and Flask snapshot synchronized as implementation lanes land. Deferred scratch routes must stay non-blocking unless explicitly promoted. | `scripts/route_snapshots.py --check` passes, `apps/api/tests/route-parity.mjs` passes, and any route-seed status changes are documented. |
| Browser JSON compatibility endpoints | Route parity | `parity-inventory.md` identifies Gen2 calls to `/campaigns/<slug>/global-search`, `/global-search/preview`, `/session/wiki-lookup/search`, and `/session/wiki-lookup/preview`; `browser-json-compatibility-decision.md` records the 2026-06-27 TypeScript compatibility implementation and focused fixture tests. | Closed for these four routes; no Gen2 `/api/v1` migration is required for this gate. | Keep `apps/api/tests/browser-json-compatibility.mjs`, `apps/api/tests/route-parity.mjs`, and route snapshots green as adjacent search/session route work lands. |
| Character create/edit parity | Route parity | Xianxia create and bounded DND-5E PHB Fighter/Barbarian plus PHB Cleric/Life Domain and PHB Wizard level-one create slices are implemented; the Cleric slice writes prepared spell selections, Life Domain always-prepared spells, and first-level spell slots, while the Wizard slice writes selected cantrips, six selected spellbook spells, selected prepared spellbook spells, Arcane Recovery, and first-level spell slots. Advanced Editor supports reference/proficiency/stat-adjustment/recoverable-penalty/custom-feature/manual-equipment slices. | Full DND level-one builder write parity, broader class/species/background choices including Sage for the Wizard fixture path, broader spellcasting classes, and broader level-one subclass choices remain pending. Linked custom-feature page/choice/spell derivation, full native edit derivation, and complete Advanced Editor parity remain pending. | Golden Flask-vs-TypeScript tests for full native DND create/edit paths, including definition/import YAML, SQLite state reconciliation, linked derivation, and stale revision behavior. |
| Character advancement parity | Route parity | Retraining now has the first bounded DND-5E save-parity slice: ready-state linked custom-feature context hydration, structured-choice validation, copied-fixture definition/import YAML writes, SQLite state revision bumps, and `native_progression` retrain history are covered by focused Flask-vs-TypeScript golden proof. Level-up now has a bounded TypeScript-created DND-5E Fighter/Barbarian level-one to level-two fixture save slice with ready context hydration, HP Gain validation, copied-fixture definition/import YAML writes, SQLite state revision bumps, HP/Hit Dice/resource reconciliation, and `native_progression` `hp_baseline` plus `level_up` history evidence. Progression-repair now has a bounded imported DND-5E class/subclass/species/background ref context/save slice with copied-fixture YAML writes, SQLite revision bumps, and `native_progression` repair history evidence. | Broader level-up parity remains pending for multiclassing, subclass choices, ASI/feat choices, spell growth, imported-sheet repair, and non-TypeScript-created/native sheets. Broader progression-repair parity remains pending for legacy spell-mark repair, feat/optional-feature repair backfills, and full imported-sheet derivation, along with broader retraining beyond linked custom-feature structured choices. | Golden tests for broader level-up and progression repair on ready and repairable DND sheets, plus any widened retraining cases; fixture SQLite/file assertions and Gen2 smoke coverage for each new advancement family. |
| Character detail/presenter parity | Route parity | Detail reads return raw definition/state, controls metadata, portrait references, and a bounded CharacterPane presenter cluster with smoke coverage for DND overview/abilities/proficiencies/equipment/inventory/spellcasting and Xianxia identity/resources/active-state/inventory/approval basics consumed by Gen2 Character, Session Character, and Combat selected-PC panes. | Full Flask presenter derivation remains outside the current detail-read slice, including richer linked Systems/page body resolution and broader DND/Xianxia presenter sections. | Golden Flask-vs-TypeScript fixture comparisons for widened DND and Xianxia presenter sections before claiming full presenter parity. |
| Missing-resource/error shape parity | Route parity | `route-snapshots.md` captures a first missing-resource matrix and notes many Flask `abort(404)` paths still return HTML. | Matrix must expand as additional cutover-critical route families are selected. Avoid accidentally normalizing Flask-compatible response shapes without an API-version decision. | Missing-resource parity tests per selected family, with explicit approval for any deliberate JSON-normalization break. |
| Fixture-only write routes stay non-production | Data migration | README records many Hono writes validated only against disposable fixture databases or copied fixture campaign trees. | Fixture validation does not prove staging or production write safety for real SQLite/content volumes. | For each write family, copied-data or staging-volume backup, mutation, restore, and equivalence transcript recorded under this evidence folder. |
| Content-character write/delete | Data migration | `content-character-staging-readiness.md` labels the family `copied-data rollback ready; staging snapshot required`; tests prove DND/Xianxia fixture parity and copied-data backup/restore. | Not yet staging-write enabled. Needs approved staging-volume snapshot with realistic files, portrait assets, `character_state`, and `character_assignments`. | Snapshot rehearsal transcript proving backup, mutation behavior, restore, and post-restore equivalence for DND-5E and Xianxia if both are present. |
| Session/runtime writes | Data migration | Session messages, start/close, articles, logs, and image reads have disposable fixture DB validation. `session-runtime-copied-data-rehearsal-2026-06-28.md` records a no-live copied-fixture backup/mutate/restore pass covering lifecycle close/start, global/DM-only/player-targeted chat, audience filtering, unchanged revision polling, staged article create/update/reveal/delete, embedded image serving, closed-log delete, restore-target API smoke, and restore equivalence. | Not yet staging-write enabled. Needs a user-approved staging-equivalent snapshot rehearsal before the family can claim `staging snapshot ready`. | Staging snapshot transcript covering live session, DM session, audience-filtered chat, staged/revealed article images, log delete, revision polling, restore equivalence, and any realistic data differences not present in sanitized fixtures. |
| Combat writes | Data migration | Combat tracker reads/writes, combatant CRUD, vitals/resources, NPC resources, conditions, and turn flow have disposable DB validation. `combat-copied-data-rehearsal-2026-06-28.md` records a no-live copied-fixture backup/mutate/restore pass covering player-character mirrors, source-backed NPC resources, revisions, conditions, delete/clear behavior, SQL samples, and player/DM/live-state payload equivalence. | Not yet staging-write enabled. Needs a user-approved staging-equivalent snapshot rehearsal before the family can claim `staging snapshot ready`. | Staging snapshot transcript covering player Combat, DM Status, DM Controls, selected-PC state writes, revisions, source-backed resources, conditions, clear/delete behavior, restore equivalence, and any realistic data differences not present in sanitized fixtures. |
| Systems and shared-source writes | Data migration | Source policy, overrides, custom entries, item mechanics import, and shared DND-5E import have disposable DB validation. `systems-copied-data-rehearsal-2026-06-28.md` records a no-live copied-fixture backup/mutate/restore pass covering source policy, proprietary acknowledgement/admin-private visibility, slashy entry overrides, custom entry lifecycle, item mechanics import/refresh, sanitized shared DND-5E import, import history, and restore equivalence. | Not yet staging-write enabled. Needs a user-approved staging-equivalent snapshot rehearsal before the family can claim `staging snapshot ready`. | Staging snapshot transcript covering realistic Systems library/policy rows, import history, custom entries, overrides, proprietary/public visibility gates, shared-source import decision, and backup/restore equivalence. |
| DM Content writes | Data migration | Statblock and custom condition mutations have disposable DB validation. `dm-content-copied-data-rehearsal-2026-06-28.md` records a no-live copied-fixture backup/mutate/restore pass covering parser output changes, condition duplicate validation, actor columns, deleted-record payloads, Combat source-backed statblock seeding, custom condition options, and restore equivalence. | Not yet staging-write enabled. Needs a user-approved staging-equivalent snapshot rehearsal before the family can claim `staging snapshot ready`; no DM Content-specific audit event path was present in the copied fixture seed beyond durable actor columns. | Staging snapshot transcript covering realistic DM Content statblocks/conditions, parser outputs, condition duplicates, actor/audit rows if present, Combat source-backed seeding, restore equivalence, and any realistic data differences not present in sanitized fixtures. |
| Published content config/pages/assets | Data migration | Content config, page, and asset writes have copied fixture validation; content pages include backlink blockers. `published-content-copied-data-rehearsal-2026-06-28.md` records a no-live copied-fixture backup/mutate/restore pass covering content config PATCH, page create/update/unpublish/republish/delete, asset upload/read/delete, protected asset serving, backlink hard-delete blocking, forced delete, stable copied SQLite publishing table counts, restored API samples, and restore equivalence. | Not yet staging-write enabled. Needs a user-approved staging-equivalent snapshot rehearsal before the family can claim `staging snapshot ready`; realistic character/session provenance blockers, live publication backup/sync/rollback, and staging/live `campaign_pages` / `campaign_page_sync_state` refresh proof remain open. | Staging snapshot transcript covering copied campaign content plus realistic config, page publish/edit/unpublish/delete, asset upload/delete, protected asset serving, provenance blockers where present, read-model refresh, and restore equivalence. |
| Image and portrait behavior | Data migration | TypeScript portrait route deliberately preserves PNG/JPG/GIF/WEBP bytes and does not add `sharp`; focused fixture proof now covers existing WebP profile references, exact PNG/JPG/GIF/WEBP upload bytes, protected asset serving, profile/import persistence, revision bumps, and old-asset cleanup. Flask production has WebP conversion paths for browser publication/portrait-related workflows. | Need a cutover decision: preserve current Flask WebP conversion, migrate data/clients, or accept extension-preserving behavior for selected API routes. Direct legacy character portrait URL behavior also remains a cutover decision because TypeScript detail payloads still advertise `/campaigns/:slug/characters/:characterSlug/portrait` while the protected-serving proof uses `/campaigns/:slug/assets/:assetRef`. | Documented decision plus conversion or extension-preserving tests for the chosen behavior, direct portrait URL/payload resolution, and any data migration for existing PNG/WebP portrait assets. |
| SQLite schema/migration layer | Data migration | Drizzle spike proves generate/migrate and runtime probes with `better-sqlite3` and `libsql`; parity inventory counts 34 unique current SQLite tables and migration guards. `sqlite-migration-dry-run-2026-06-28.md` records copied synthetic current-style SQLite evidence for guarded Flask `init-db`, additive/idempotent schema initialization, 34-table post-migration shape, WAL/index/pragma observations, TypeScript compiled API read/startup smoke, backup restore into a separate target, and restore equivalence. `sqlite-startup-posture-2026-06-28.md` records the transitional handoff: Flask `manage.py init-db` remains the initializer, TypeScript startup preflights the current schema before serving, and shared SQLite opens apply intentional PRAGMAs including `foreign_keys=ON`, WAL/normal synchronous for writable opens, and Flask-matching 30000 ms busy timeout. | Full production/staging migration posture is still blocked until a tracked TypeScript/Drizzle migration command or equivalent additive migration hook exists and is rehearsed. `apps/api/src/content/characterAuthoring.ts` still needs helper adoption after the Wizard create parity lane lands. | User-approved staging-equivalent migration rehearsal covering realistic current DB shape, chosen TypeScript or transitional schema command, WAL/busy-timeout/index/additive migration checks, failed/partial migration rollback, and post-migration TypeScript startup/read smoke. |
| Local Windows ops | Ops/deploy | Current contract uses `local.ps1`, workspace venv or bundled/runtime-specific tooling, and `.local/tmp` scratch boundaries; `local.ps1 -Action ts-api-check` now resolves Node/npm without global `npm` and runs install, route snapshot, typecheck, build, SQLite startup-posture test, and route-parity checks. | The API typecheck/build/route-parity/startup-posture wrapper gap is closed for local Windows validation. Local compiled-start smoke and Docker/Fly packaging proof remain separate gates. | Keep the wrapper green on integration branches, and continue collecting local compiled-start, container, `/healthz`, `/app-next/`, asset, and rollback evidence before deploy or cutover approval. |
| Docker/Fly packaging | Ops/deploy | Flask Fly shape is one app, one `/data` volume, SQLite at `/data/player_wiki.sqlite3`, campaign content at `/data/campaigns`, and `/healthz`. The Dockerfile now has a non-default `ts-api-runtime-proof` target and static packaging proof for TypeScript API build/start/env mapping. The 2026-06-28 no-deploy pass also proved the compiled TypeScript API can start locally from `apps/api/dist/server.js` against copied fixture data and answer `/healthz` plus `/api/v1/app` with the expected disposable path metadata. A later 2026-06-28 pass on integration commit `61191fe17651309dfdad9f9c42c26cd6cc4c5be0` kept `ts-api-check` and static packaging proof green, but a compiled-start proof from a Flask-initialized scratch DB failed before serving because TypeScript startup preflight expected `sessions.session_token_hash` while Flask initialized `sessions.token_hash`. Hono stack spike did not run Fly deploy. | TypeScript local image build, container startup command execution, schema initialization, frontend build integration, and sanitized `fly.toml` behavior still need runtime packaging proof. Docker was unavailable for the current no-deploy image build, and the Flask-schema/preflight token-column mismatch must be resolved or deliberately documented before compiled-start proof is reproducible from `manage.py init-db`. | Local production build plus container smoke, `/healthz`, `/app-next/` if frontend is bundled, representative asset content type, and no tracked private Fly identifiers. |
| Backup, restore, rollback | Ops/deploy | Charter requires rollback to Flask until at least one full rehearsal and approved observation window. Ops docs preserve guarded backup/restore/sync behavior. `rollback-cutover-runbook.md` and `staging_rehearsal_harness.py guide --family rollback-cutover` now provide the no-live transcript scaffold for last known-good Flask target, pre-cutover DB/content backup, restore command shape, data-delta decisions, and Flask health smoke. | Need a completed copied-data or user-approved staging-equivalent rollback transcript. The scaffold is not proof by itself. | Rehearsed rollback transcript from copied/staging data, including restore, Flask health smoke, and decision tree for writes accepted by TypeScript before rollback. |
| Full cutover workflow smoke | Rehearsal | Charter names the required user workflows across auth, Campaign Home/wiki/search/help, DM Content, Systems, Characters, Session, Combat, and ops. The `rollback-cutover` harness family now captures the full smoke checklist in the same transcript as rollback evidence. | No completed full rehearsal transcript yet. Route-family staging snapshot gates must pass before this family can claim `cutover rehearsal passed`. | Browser/API smoke pass on copied or staging data covering every charter workflow, with failures classified and linked to follow-up lanes. |
| Freeze and dual-maintenance | Rehearsal | Charter allows ongoing Flask work until hard freeze, but new Flask behavior must update current-state docs and parity inventory or be an approved exception. | Need a cutover-rehearsal freeze checklist and owner for parity-inventory drift review. | Final pre-rehearsal audit showing current-state docs, `docs/api-v1.md`, snapshots, and TypeScript parity inventory agree. |

## Readiness Labels

Use these labels consistently in follow-up evidence:

- `route-parity complete`: TypeScript route behavior is covered by source snapshot checks, API/client contract checks, and golden Flask comparison where behavior is non-trivial.
- `fixture-write validated`: TypeScript write behavior is proven only against disposable fixture SQLite or copied sanitized fixtures. This is not staging or production approval.
- `copied-data rollback ready`: A write family has backup/mutate/restore equivalence on copied realistic-enough data, but has not touched staging/live routing.
- `staging snapshot ready`: A user-approved staging-equivalent snapshot rehearsal has passed and the transcript is tracked.
- `cutover rehearsal passed`: All charter workflows pass against copied or staging data, rollback is rehearsed, and Flask hard-freeze exceptions are resolved.

## Parallel Lane Queue

These lanes can proceed without conflicting with this docs-only readiness lane:

| Lane | Owns | Avoids |
| --- | --- | --- |
| Route parity evidence lane | `apps/api/tests`, route snapshot fixtures, family-specific golden parity tests, and route-seed status updates. | Runtime handler edits unless the lane explicitly owns the affected `apps/api/src/**` module cluster. |
| Character authoring parity lane | DND full create, Advanced Editor derivation, retraining, level-up, and progression-repair implementation/tests. | Ops/deploy files and unrelated Combat/Session handler clusters. |
| Staging rehearsal harness lane | New rehearsal docs/scripts under `docs/typescript-backend-rewrite/` and disposable `.task-temp` harnesses for backup/mutate/restore transcripts. | Active runtime handler files; live data; `.local/roadmaps` unless explicitly requested. |
| Ops packaging lane | TypeScript local run/build/check wrapper decisions, Docker/Fly packaging proof, `/healthz`, startup migration command shape, rollback runbook. | Route implementation files and live deploy/sync unless explicitly approved. |
| Browser compatibility lane | Gen2 migration from browser JSON compatibility endpoints or TypeScript compatibility route tests for global search and session wiki lookup. | Character authoring runtime files unless the scope is expanded. |
| Image/data policy lane | Portrait and publication image conversion decision, media-type tests, data migration notes for existing assets. | Live content volumes and production publication flows without explicit approval. |

## Immediate Orchestrator Recommendations

1. Queue character authoring parity next if the goal is route-semantic completeness: full DND create, Advanced Editor derivation, and advancement save parity are the clearest implementation blockers.
2. Queue a staging rehearsal harness in parallel with implementation: it can collect backup/restore command scaffolding and transcript templates without touching active runtime files.
3. Queue an ops packaging proof before any deploy discussion: TypeScript must prove local production build, container/Fly startup shape, mounted volume paths, and rollback instructions.
4. Keep `deferred_scratch_proof` routes out of cutover scope unless a later architecture decision promotes session-cookie auth, generic upload, or generic notes APIs.
5. Before a user-approved PR/merge/deploy/cutover step, run a final drift pass across `docs/current-state/INDEX.md`, `docs/api-v1.md`, `route-snapshots.json`, `typescript-route-seed.json`, and this matrix.

## Non-Goals For This Lane

- No production deploy.
- No live SQLite or Fly volume sync.
- No PR creation or merge.
- No change to Flask production authority.
- No edits to ignored roadmap files.
- No runtime implementation changes.
